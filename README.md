# Gemini Cost Guardian

A deterministic scenario runner (“Reliability Lab”) that produces traces + metrics for Datadog monitors (latency/error/security/cost/tool-abuse) and supports a reproducible demo via a traffic generator.

## Local development

```bash
npm install
# set .env.local (DO NOT COMMIT)
npm run dev
```
